const maiorValor = retornarMaiorValor(1,2);

function retornarMaiorValor(a,b){
    if(a > b)
        return a;
    
    else(a < b)
        return b;}

console.log(maiorValor);

// Precisamos comparar dois valores para mostrarmos o maior entre eles.
// Passo 1: Criar uma variável para receber os valores.
// Passo 2: Criar a função para comparar os valores.
// Passo 3: Exibir o restultado (da variável criada) no console.